# --- Private notes: never pushed to the public repo ---
# Ignore all note CONTENT under notes/, but keep the folder structure,
# the .gitkeep markers, and the README files so a clone shows the layout.
notes/**
!notes/**/
!notes/.gitkeep
!notes/README.md
!notes/**/.gitkeep
!notes/**/README.md

# Secrets / local config
.env

# OS / editor junk
.DS_Store
*.swp
.idea/
.vscode/

# Local GBrain dir if you ever point it inside the project
.gbrain/
