# Example notes (public)

Sample notes that ship with the repo, grouped by area, so a fresh clone has
something to import and test with. They are generic and safe to be public.

Load them all with:

```bash
make seed
```

Your own real notes go in `../notes/<area>/`, which is git-ignored and private.
